'use client';

import AICVInterview from './AICVInterview';

/** The builder is intentionally one short, conversational interface. */
export default function BuilderShell() {
  return <AICVInterview />;
}
